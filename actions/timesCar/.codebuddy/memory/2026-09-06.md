# 2026-09-06

- 解析 `mail/【Times CAR】予約登録完了.eml`（Times CAR 租车预约确认邮件）。
  - 编码：ISO-2022-JP + quoted-printable，需 `email` 模块解码才可读。
  - 关键信息：车型 ソリオ（铃木 Solio，银色，车牌 葛飾500わ2334）、预约号 205227027、站点 ＭＧＡ金町、借用 2026/01/30 11:30–14:00、预计 2200 日元。

- 用户询问是否需要安装 eml VSCode 插件辅助阅读。结论：**不需要**，agent 可直接用 Python `email` 模块（`get_payload(decode=True)` + `get_content_charset()`）解码任意编码的 eml；插件只对用户自己预览有用。
- 提议（待用户确认）：在工作区放一个小脚本，一键输出 eml 解码正文 + 关键头部字段。

- 新增 `parse_mail.py`：批量解析 `mail/*.eml` → 生成 `formMail.md`。
  - 输出 4 列：利用開始日時 / ステーション / 車両 / 予約番号。
  - 规则：按予約番号去重；**按邮件 Date 头升序处理，后处理覆盖先处理**，使「予約変更完了」覆盖「予約登録完了」；最终按利用開始日時升序输出。
  - 现状：mail 目录 71 封（1 封変更 + 70 封登録）→ 去重后 70 条，时间跨度 2026/01/30 – 2026/09/06。
  - Times CAR 邮件字段格式：`■予約番号` 等标签单独一行，值在下一行（站点下一行还有 URL，需跳过 http 开头的行）。
  - 邮件实际有 71 封（1 封「予約変更完了」+ 70 封「予約登録完了」）。

- 新增 `site/` 数据源整合（8 个 `details_YYYYMM.csv`，**cp932/Shift-JIS 编码**，覆盖 2026/01–2026/08）。
  - CSV **没有「予約番号」列**！列：項目名,予約開始日時,返却予定日時,利用開始日時,利用終了日時,キャンセル日時,ICカード,会員名,利用時間,走行距離,料金プラン,クラス,ステーション,ペナルティ,請求金額。
  - 关联方案：`mail.利用開始日時`(即预约开始) == `CSV.予約開始日時` 且 `ステーション` 相同 → 补回 予約番号。
  - CSV 只取 `項目名 == "利用料金"` 的行作为一次预约主记录（另有 安心補償サービス/月額基本料金/無料利用料金/割引票 等，均排除）。
  - `利用時間` 单位=分钟，且是「予約開始日時 → 実際利用終了」的计费时长，不等于实际用车时长。
  - `parse_mail.py` 已升级为 mail+site 合并，输出 formMail.md（仅保留双方都有的）+ 两个未匹配清单。
  - 结果：匹配 54 条；邮件独有 16 条（多为改约/取消或 9 月无 CSV）；CSV 独有 1 条（2026/05/16 タイムズ南与野第４，无对应邮件）。
  - 用户要求：这三列用 **CSV 原始值**（利用時間=分钟整数、走行距離=km、請求金額=円），列名保持 `利用時間(分) | 走行距離(km) | 請求金額(円)`。

- 新增 `build_web.py` → 生成 `web/index.html`（单文件、零外部依赖的租车数据面板）。
  - 数据源：解析 `formMail.md` 主表（9 列）→ 内嵌 JSON。重跑 `python build_web.py` 即可刷新。
  - 车型提取规则：車両列去掉末尾含车牌号的括号部分（按 ` （` 分隔，因车型名如 `ヤリス（ハイブリッド）` 的括号前无空格）。
  - 车系归一：`base_model()` 去掉尾部括号 → アクア/オーラ/スイフト/ソリオ/ノート/フィット/ヤリスクロス/ヤリス/MAZDA2（11 型号 → 9 车系）。
  - 页面：暗色玻璃拟态 + 霓虹光斑，KPI 数字滚动、车型卡片网格、可切换对比图（距离/金额/时间/次数）、月度柱状趋势、可搜索排序的行程明细表。
  - 校验方式：抽出内嵌 JS 用 `node --check` 做语法冒烟测试。
  - 车标：用户提到 chebiao.net，但该站图片 URL 需 JS 渲染、抓不到直链 → 改用**内联 SVG 车标**（Toyota 三椭圆 / Nissan 圆环+字 / Suzuki S 曲线 / Honda 梯形H / Mazda 圆M）。
  - 品牌映射（`BRAND` 字典，按 base 车系）：アクア・ヤリス・ヤリスクロス→Toyota，ノート・オーラ→Nissan，スイフト・ソリオ→Suzuki，フィット→Honda，MAZDA2→Mazda。分布：Suzuki21/Toyota20/Honda7/Nissan5/Mazda1。
  - 车标覆盖机制（**已修 bug**）：最初 img 直接写在 HTML 里、SVG 垫在下面，导致两者叠加。现改为 **JS 惰性加载**：`logo()` 只输出 SVG(class=`fb`)，渲染后调用 `upgradeLogos(root)` 用 `new Image()` 依次尝试 `logos/<brand小写>.{png,svg,jpg,jpeg,webp}`，加载成功才插入 `.pic` 并给容器加 `.hasimg`，CSS `.logo.hasimg .fb{display:none}` 隐藏手绘 SVG。
  - 每次重渲染（卡片/对比图/表格/图例）后都必须调用 `upgradeLogos(...)`；`data-done` 标记防重复。
  - **SVG 车标支持（关键）**：用户提供的 `web/logos/toyota.svg` 是 Illustrator 导出，**无 fill 属性（默认黑色）+ 无 width/height（只有 viewBox）**，在深色背景上不可见。
    - 方案：SVG 用 **CSS mask 裁形 + 品牌色填充**（`.logo.masked{background:var(--bc);mask:var(--mask) center/contain no-repeat}`），绕开原始填充色与尺寸缺失问题，矢量任意缩放。
    - 位图仍走 `<img class="pic">` 原色显示。LOGO_EXTS 顺序 `['svg','png','webp','jpg','jpeg']`（svg 优先）。
    - UI 增「车标·品牌色 / 车标·原色」切换（`#tint`），选择存 localStorage(`logo-tint`)；`refreshLogos()` 全量重着色。
  - **最终方案（已修「浏览器不显示」）**：改为 **构建期内联**。`build_web.py::load_logo_files()` 扫描 `web/logos/`，在构建时把 SVG 文本清洗后嵌入 `const LOGO_FILES = __LOGOS__`，运行时直接 `innerHTML` 注入 → 不依赖 fetch/CORS/CSS mask，`file://` 双击必显示。
  - 清洗步骤：去 XML 声明/DOCTYPE/注释 → 去 `width`/`height`/`style` 属性 → 补 `preserveAspectRatio`；无 `viewBox` 则跳过。
  - 着色：构建期检测 `mono`（无彩色 fill）与 `stroked`（含 stroke=）。品牌色模式 `.pic svg,.pic svg *{fill:var(--bc)}`；原色模式单色图标 → `#0f172a` + 浅色徽章底，彩色图标 → `fill:revert`。
  - **注意**：用户 SVG 常是 Illustrator 导出，坑点 = 无 fill（默认黑）/ 根 `fill="none"` 且 path 无 fill（全隐形）/ 根 `fill="#000000"`。强制 CSS fill 可一并解决。
  - 现状：web/logos 有 toyota.svg、honda.svg、nissan.svg、suzuki.png（4 个）。新增/更换车标后需重跑 `python build_web.py`。

- **网页风格偏好（用户明确要求）**：暗色保留，但「不要太高调」。已将 `build_web.py` 的 TEMPLATE 样式从「霓虹高对比」改为**克制暗色磨砂玻璃**：
  - 背景光晕透明度 0.5→0.16、偏冷蓝紫、动画 22s→34s；标题渐变改为柔和冷色（去掉霓虹粉）。
  - 统一玻璃质感：KPI/卡片/对比图/月度/明细/搜索框/图例/分段控件 全部 `--glass`(rgba(255,255,255,.055)) + `backdrop-filter:blur(22px) saturate(140%)` + 内高光 `--hi`(inset 0 1px 0) + 柔阴影 `--shadow`。
  - 分段选中态改为浅色玻璃（非霓虹），卡片悬停发光由彩色外发光改为柔和阴影，角标光晕 0.24→0.12。
  - 后续若再改风格，默认方向=内敛玻璃、低饱和、少发光。

- 车型照片功能（用户把 11 张车图放进 `web/car/`，要求加到卡片）：
  - `build_web.py` 新增 `CAR_IMG_MAP`（文件名英文小写→base 车系：aqua→アクア / aura→オーラ / fit→フィット / mazda2→MAZDA2 / note_e-power→ノート / solio→ソリオ / swift→スイフト / yaris→ヤリス / yariscross→ヤリスクロス）。
  - `load_car_images()` 扫描 `web/car/`（png/jpg/jpeg/webp，同名优先 png），构建期生成 `{"车系":"car/文件名"}` 注入 `__CARIMGS__`。
  - 卡片顶部加 `.cover` 封面：整宽照片 + 底部渐隐（融进暗色）；`.rk` 排名浮于封面之上带投影。`groupBy` 对象已加 `base` 字段便于查图。图片用相对路径 `<img>`,file:// 直接显示。
  - 用户目录实际文件名：aqua.png, aura.jpg+aura.png, fit.png, mazda2.png, note_e-power.jpg+note_e-power.png, solio.png, swift.png, yaris.png, yariscross.png（共 11 文件，9 车系）。
  - 用户后来重新提供了ノート/オーラ的透明背景新车图，覆盖到 web/car/note_e-power.png 和 web/car/aura.png。
  - 卡片布局要求：顶部标题，左侧车型小图，右侧行驶数据。已把 build_web.py 卡片从「全宽封面」改为「标题行 + 左侧 110px 车型缩略图 + 右侧 chips/统计/条形图」，缩略图带径向高光背景确保透明 PNG 可见。
